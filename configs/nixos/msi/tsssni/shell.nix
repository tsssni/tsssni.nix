{ ... }:
{
  tsssni.shell = {
    fetch.enable = true;
    prompt.enable = true;
    shell.enable = true;
    terminal = {
      enable = true;
      extraSettings = {
        font_size = "16.0";
      };
    };
  };
}
